<configuration>

  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{40} -- %msg%n</pattern>
    </encoder>
  </appender>

  <!-- HA Agent business modules -->
  <logger name="com.neo4j.ha" level="INFO" />

  <!-- Jetty / Javalin: suppress HTTP internals -->
  <logger name="org.eclipse.jetty" level="WARN" />
  <logger name="org.eclipse" level="WARN" />
  <logger name="io.javalin" level="WARN" />

  <!-- Neo4j Driver internals -->
  <logger name="org.neo4j.driver" level="WARN" />

  <!-- Redis / Jedis -->
  <logger name="redis.clients" level="WARN" />

  <root level="INFO">
    <appender-ref ref="CONSOLE" />
  </root>

</configuration>
